# Handle optional dependencies properly
optional=true

# Allow installing optional dependencies
save-optional=true

# Prevent optional dependency failures from breaking the install
ignore-optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Force install when there are conflicts
force=false
