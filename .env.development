# Frontend (Vite) development environment overrides
# Point API calls to backend that is currently running on port 3001
VITE_API_URL=http://localhost:3001/api

# If you ever switch the backend back to 3000, change above accordingly.
# Restart the Vite dev server after editing this file for changes to take effect.
